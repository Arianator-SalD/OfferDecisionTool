# OfferDecisionTool 迭代更新记录

按日期聚合记录项目中已经确认的产品与工程迭代，最新更新在前。

## 待迭代

- 实时推荐解释增强：在推荐结果中补充“为什么该 offer 胜出”的贡献拆解，例如主要优势、主要短板和高权重维度影响，避免只展示一个加权分。
- 桌面效率能力补齐：支持复制 offer、清空某个 offer 分数、从 CSV 或表格粘贴导入，并考虑新增常用快捷键。
- 权重预设能力：提供保守型、成长型、薪资优先、生活平衡优先等预设，帮助用户快速开始调权重。
- 用户文案收敛：将“推荐 JSON”“Supabase”等偏工程实现的词替换成用户更容易理解的表达，例如“导出决策档案”“云端同步设置”。
- 桌面端设计验收：以 1280px 和 1440px 宽度作为主要验收视口，重点检查首屏层级、右侧推荐栏、表格可读性、按钮对比度和焦点状态。
- 后续响应式储备：移动端横向滚动打分表暂不作为桌面版发布阻塞项，但保留为未来 `adapt` 方向，后续可设计独立的移动端比较流程。

## 2026-07-29

- 将档案切换、新建、重命名、删除压缩到统一档案菜单，保留隐藏原生 select 作为状态兼容层。
- 移除前台“导出推荐 JSON”入口，保留内部推荐 JSON 作为 AI 决策总结输入。
- 将决策总结的生成中、成功、失败、数据更新提醒统一迁移到全局 toast。
- 统一桌面视觉系统，并进一步调整为与个人网站同源的印象派风格：雾蓝、莲粉、奶油底与深青灰。

## 2026-07-12

- 优化 Supabase 登录错误提示，减少 `Failed to fetch` 这类原始报错带来的排查成本。
- 区分网络连接失败、Supabase 项目 URL 不可解析、账号密码错误、邮箱未确认等常见登录失败场景。
- 将 `Invalid login credentials` 等原始英文错误转成更清晰的中文提示，方便后续定位登录和云同步问题。

## 2026-05-16

- 初始化 OfferDecisionTool 项目，完成基础 offer 打分表、维度权重、实时排序、雷达图和本地保存能力。
- 接入 GitHub 与 Vercel 发布链路，并排查“push 后 Vercel 未更新”的提交流程问题。
- 新增“职业选择档案”能力，支持新建、切换、重命名、删除档案；每个档案独立保存 offer、权重、备注和推荐结果。
- 将产品定位从 offer 打分表升级为 `Career Decision Journal / 职业选择档案`，强调记录每一次选择的理由、偏好与逻辑。
- 优化档案交互：自定义档案下拉、统一应用内弹窗、删除档案和删除 offer 前二次确认，降低误触风险。
- 将“工作强度”维度调整为“工作生活平衡”，让该维度语义更适合职业选择场景。
- 新增推荐 JSON 导出能力，输出完整维度、权重、offer 排名、加权贡献和最终推荐。
- 明确 AI 总结方案：推荐 JSON 只保留事实数据，prompt 放在后端，AI 只负责解释系统已经算出的推荐结果。
- 新增 Supabase Edge Function `generate-decision-summary`，并切换为 DeepSeek 模型生成 2-3 句中文决策总结。
- 前端新增“生成决策总结”按钮，用户登录后可调用后端函数生成当前档案的 AI 摘要。
